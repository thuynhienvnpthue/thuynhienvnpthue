import random
total_flips = 0
num_tails = 0
num_heads = 0
for _ in range(1000):
  n = random.random()
  if n < 0.5:
    num_tails = num_tails + 1
  else: 
      num_heads = num_heads + 1
  total_flips = total_flips + 1
print(num_tails/total_flips)
